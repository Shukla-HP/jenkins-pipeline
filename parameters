pipeline {
    agent any
    parameters {
        booleanParam defaultValue: true, name: 'RUN_PROJECT'
}

    stages {
        stage('STAGE1') {
            steps {
               sh 'echo "$(params.RUN_PROJECT)"'   
            }
        }
    }
}
